# Khosha Monorepo

This repository contains two projects:

- `RetailerOS/` — RetailerOS application (migrated from previous repo root)
- `KMS/` — KMS (Khosha Management Systems) project management app
